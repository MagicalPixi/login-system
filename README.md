### server

#### Install submodule

```bash
git submodule update
```

#### Update submodule

```bash
cd <submoduledir>
git pull
```
or if your are busy. do like this

```bash
git submodule foreach git pull origin master
```
